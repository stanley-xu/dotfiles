# Terminal prompt
# Need to skip if on Terminal.app
if [ "$TERM_PROGRAM" != "Apple_Terminal" ]; then
  eval "$(oh-my-posh init zsh --config ${HOME}/.config/ohmyposh/prompt.toml)"
fi

export DOCKER_HOST="unix://$HOME/.config/colima/default/docker.sock"
